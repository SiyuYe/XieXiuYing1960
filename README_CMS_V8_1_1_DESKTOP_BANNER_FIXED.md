# CMS V8.1.1：桌面版橫幅固定高度修正

## 本次只修改

- 桌面版（寬度 901px 以上）首頁與各頁共同 Header 橫幅。
- 橫幅固定高度為 410px，不再隨瀏覽器寬度持續放大或縮小。
- 圖片維持原始比例，不拉伸、不變形。
- 圖片置中顯示；螢幕比圖片顯示寬度更寬時，左右以 `#f4e9dd` 補底。
- 手機版與既有 900px 以下規則不更動。
- 全站 HTML 的 CSS 快取版本更新為 `style.css?v=8110`。

## 修改檔案

- `style.css`
- 所有引用 `style.css` 的 HTML（僅更新快取版本字串）
